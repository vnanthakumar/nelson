#include<stdio.h>
#include<cinio.h>
void main()
{
   int b ;
   scanf( "%d",&b);
   if(0>a)
   {
      printf(" positive");
    }
    else(0<b)
   {
     printf("negative");
     }
    getch();
    }
