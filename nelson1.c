#include<stdio.h>
#include<cinio.h>
void main()
{
   int a ;
   scanf( "%d",&a);
   if(0>a)
   {
      printf(" positive");
    }
    else(0<a)
   {
     printf("negative");
     }
    getch();
    }
